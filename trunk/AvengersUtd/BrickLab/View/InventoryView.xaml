<UserControl x:Class="AvengersUtd.BrickLab.View.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:AvengersUtd.BrickLab.ViewModel"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:Controls="clr-namespace:AvengersUtd.BrickLab.Controls" mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="800"
             Background="{StaticResource DefaultBackground}" Loaded="UserControl_Loaded">
    <UserControl.Resources>
        <Controls:ColorIdConverter x:Key="ColorIdConverter"/>
        <Controls:ColorIdNameConverter x:Key="ColorIdNameConverter"/>
    </UserControl.Resources>
    <DataGrid Name="InventoryGrid" 
              AutoGenerateColumns="False" VerticalAlignment="Top" ItemsSource="{Binding PartList}" CanUserDeleteRows="True" SelectionMode="Extended" SelectionChanged="InventoryGrid_SelectionChanged">
        <i:Interaction.Triggers>
            <i:EventTrigger EventName="SelectionChanged">
                <i:InvokeCommandAction Command="{Binding SelectionChangedCommand}"
                                       CommandParameter="{Binding SelectedItems,
                            ElementName=InventoryGrid}"/>
            </i:EventTrigger>
        </i:Interaction.Triggers>
        <DataGrid.Columns>
            <DataGridTemplateColumn>
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <Image Source="{Binding ImageUri}" Height="24" Margin="-2"/>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
                
            </DataGridTemplateColumn>
            <DataGridTextColumn Header="Item #" Width="64" IsReadOnly="True" Binding="{Binding ItemNr}" />
            <DataGridTextColumn Header="Description" Width="320" IsReadOnly="True" Binding="{Binding Description}"/>
            <DataGridTemplateColumn Header="Color" Width="128">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Rectangle Width="32" Fill="{Binding ColorId, Converter={StaticResource ColorIdConverter}}"/>
                            <TextBlock Margin="4 0 0 0" Text="{Binding ColorId, Converter={StaticResource ColorIdNameConverter}}"
                                       Width="88" TextTrimming="WordEllipsis" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
            <DataGridTemplateColumn Header="Quantity">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Quantity}" />
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
                <DataGridTemplateColumn.CellEditingTemplate>
                    <DataTemplate>
                        <TextBox Text="{Binding Quantity,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </DataTemplate>
                </DataGridTemplateColumn.CellEditingTemplate>
            </DataGridTemplateColumn>
            <Controls:TextBoxTemplateColumn Header="Price" Width="64" IsReadOnly="False" CellStyle="{StaticResource EditableCellStyle}">
                <DataGridTemplateColumn.CellTemplate >
                    <DataTemplate>
                        <Controls:AccountingCell Amount="{Binding Price, StringFormat=0.00;;-}" />
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
                <Controls:TextBoxTemplateColumn.CellEditingTemplate>
                    <DataTemplate>
                        <TextBox Text="{Binding Price,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, StringFormat=0.00;;-}" 
                                 TextAlignment="Right" />
                    </DataTemplate>
                </Controls:TextBoxTemplateColumn.CellEditingTemplate>
            </Controls:TextBoxTemplateColumn>
        </DataGrid.Columns>
    </DataGrid>
</UserControl>
